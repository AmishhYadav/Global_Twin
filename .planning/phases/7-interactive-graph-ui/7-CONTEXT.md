# Phase 7: Interactive Graph UI - Context

**Gathered:** 2026-03-19
**Status:** Ready for planning

<domain>
## Phase Boundary

This phase expands the existing Phase 6 Dashboard Layout to visually expose the native `NetworkX` Knowledge Graph (generated in Phase 3) as a navigable, drag-and-drop structural node diagram natively embedded into the web application.
</domain>

<decisions>
## Implementation Decisions

### Rendering Library
- Implemented `pyvis` to safely render the backend `nx.DiGraph` mathematically into interactive physics HTML documents exposing structural causal topological dependencies efficiently.

### Dashboard Placement
- Bound the HTML module cleanly at the top of the interface natively using `streamlit.components.v1.html()`, to ground the end-user inside actual empirical causality rules *before* manipulating shocks.
</decisions>

<canonical_refs>
## Canonical References

### Project Scope
- `.planning/PROJECT.md` — Constraint: UI-04.
</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/dashboard/app.py` — Existing layout architecture readily accepts natively compiled physics blocks natively over `st.columns`.
</code_context>

<specifics>
## Specific Ideas
- Generate a generic static `/tmp/network_graph.html` payload natively generated by the dynamic Phase 3 DAG NetworkX topology struct natively mapping node edges to DOM bindings safely.
</specifics>

<deferred>
## Deferred Ideas
None.
</deferred>

---

*Phase: 7-interactive-graph-ui*
*Context gathered: 2026-03-19*
